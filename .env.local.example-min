# ==============================================
# AuditoryX Minimal Environment Configuration
# ==============================================
# 
# This file contains only the essential environment variables
# required to boot the Next.js application successfully.
# 
# For full feature development, use .env.example as reference.
#

# ==============================================
# REQUIRED FOR APPLICATION BOOT
# ==============================================

# Next.js Environment
NODE_ENV=development
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_minimum_32_chars

# Firebase Configuration (Required for authentication)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin (Required for server-side operations)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=service_account@your_project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ==============================================
# OPTIONAL BUT RECOMMENDED
# ==============================================

# Stripe (Required for payment functionality)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Error Monitoring (Recommended for debugging)
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# ==============================================
# FEATURE FLAGS (Development Only)
# ==============================================

# Enable incomplete features during development
ENABLE_2FA=false
ENABLE_KYC_WEBHOOK=false
ENABLE_ANALYTICS_DASHBOARD=false
ENABLE_ADVANCED_SEARCH=true
ENABLE_CHAT_ENCRYPTION=true

# ==============================================
# SETUP INSTRUCTIONS
# ==============================================
# 
# 1. Copy this file to .env.local
# 2. Replace placeholder values with your actual credentials
# 3. For Firebase credentials, get them from Firebase Console > Project Settings
# 4. For Stripe keys, get them from Stripe Dashboard > Developers > API Keys
# 5. The app will boot with minimal functionality - enable features as needed
# 
# SECURITY NOTES:
# - Never commit .env.local to version control
# - Use strong, unique secrets in production
# - Rotate secrets regularly
# - Test with minimal config first before adding optional services
# 
# ==============================================